# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Author {
  id: ID!
  name: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type HelloWorld {
  author: Author
  id: ID!
  message: String
}

type Mutation {
  createUser(nickname: String!): User!
  deleteUser(id: ID!): Boolean!
  updateUserNickname(id: ID!, nickname: String!): User!
}

type Query {
  author(id: ID!): HelloWorld!
  helloWorld(id: ID!): HelloWorld!
  user(id: ID!): User!
}

type Subscription {
  commentAdded: String!
}

type User {
  id: ID!
  joinedAt: DateTime!
  leavedAt: DateTime
  nickname: String!
  profileImageUrl: String
}